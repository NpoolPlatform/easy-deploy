- name: Lotus official source code checkout
  ansible.builtin.git:
    repo: 'https://github.com/filecoin-project/lotus.git'
    dest: '{{ ansible_env.HOME }}/filecoin/official/lotus'
    version: '{{ version }}'
    force: true
